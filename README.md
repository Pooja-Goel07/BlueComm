# BlueComm: Bluetooth Classic Based Android Messaging System

The system is a standalone Android application developed using Flutter. It enables two Android devices
within Bluetooth range to discover nearby Bluetooth Classic devices, establish an RFCOMM socket
connection, and exchange UTF-8 encoded text messages in real time. The system operates entirely offline
and does not rely on external servers, databases, or cloud services. All communication is strictly
session-based. 